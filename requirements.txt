Django>=5.2,<5.3
paho-mqtt>=2.0,<3.0
# Timezone database for Python's zoneinfo. The slim ubuntu:22.04 base image
# ships no system tzdata, so with USE_TZ=True any aware-datetime render
# (e.g. a saved pose's created_at) raises ZoneInfoNotFoundError('UTC') -> 500.
tzdata>=2024.1
